Caso acessar por uma nova máquina e receber a mensagem: 
no changes added to commit (use "git add" and/or "git commit -a")
PS C:\Users\Ruffo\Documents\curso_git_hub\primeiro_repo_1\curso_git_1> git commit -a

*** Please tell me who you are.

Run

  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

to set your account's default identity.
Omit --global to set the identity only in this repository.

Precisa somente digitar o email e o nome conforme no exemplo acima.

git config --global user.email "meu email"
git config --global user.name "meu username"

Isso só precisa ser feito uma única vez.